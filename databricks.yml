bundle:
  name: customer-support-query-bot

resources:
  apps:
    customer_support_bot:
      name: "customer-support-bot"
      description: "Customer Support Query Bot — login and email change lookups"
      source_code_path: .
